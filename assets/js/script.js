// Generated by CoffeeScript 1.6.3
jQuery(function($) {
  var $members, deg, liHeight, randomDeg, transZ;
  deg = 0;
  transZ = Math.floor($('#box').height() / 2);
  $members = $('#box ul > li');
  liHeight = Math.floor(1200 / $members.length * 100) / 100;
  $members.each(function() {
    $(this).css({
      lineHeight: "" + liHeight + "px",
      marginTop: "-" + (liHeight / 2) + "px",
      transform: "rotateX(" + deg + "deg) translateZ(" + transZ + ")",
      webkitTransform: "rotateX(" + deg + "deg) translateZ(" + transZ + "px)"
    });
    return deg += Math.floor(360 / $members.length);
  });
  randomDeg = Math.floor(Math.random() * 360);
  $('#boxWrap').css({
    transform: "rotateX(" + randomDeg + "deg)",
    webkitTransform: "rotateX(" + randomDeg + "deg)"
  });
  $('#members').fadeIn(3000);
  return $('#box').addClass('reverseList').one('webkitAnimationEnd animationend', function() {
    return $(this).removeClass('reverseList').delay(50).queue(function() {
      var pattern;
      pattern = Math.floor(Math.random() * 4) + 1;
      $(this).addClass("resetWrap anm" + pattern).dequeue();
      return $(this).find('ul').one('webkitAnimationEnd animationend', function() {
        return $('#box').addClass('finish');
      });
    });
  });
});

/*
//@ sourceMappingURL=script.map
*/
